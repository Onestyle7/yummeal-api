namespace yummealAPI.Models.Enums
{
    public enum ActivityLevel
    {
        Sedentary = 1, // Siedzący tryb życia
        LightlyActive = 2, // Lekko aktywny tryb życia (1-3 dni/tydzień)
        ModeratelyActive = 3, // Umiarkowanie aktywny tryb życia (3-5 dni/tydzień)
        VeryActive = 4, // Bardzo aktywny tryb życia (6-7 dni/tydzień)
        SuperActive = 5 // Ekstremalnie aktywny tryb życia (sport + praca fizyczna)
    }
}